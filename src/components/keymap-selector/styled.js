import Switch from 'react-switch';
import styled from 'styled-components';

export const KeymapSelectorElement = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-size: 1.4rem;
  margin: 1rem 0 0 0;

  p {
    line-height: 1.8rem;
  }
`;
export const SwitchElement = styled(Switch)`
  margin: 0.4rem 0 0 0;
`;
