import styled from 'styled-components';

export const FooterHelpElement = styled.div`
  font-size: 1.4rem;
  font-weight: 300;
  margin: 0;

  p {
    margin: 0;
  }

  span {
    font-weight: 400;
    margin-right: 0.6em;
  }
`;
